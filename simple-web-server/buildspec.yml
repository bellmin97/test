version: 0.2

phases:
  install:
    commands:
      - echo Nothing to install
  build:
    commands:
      - echo Nothing to build
  post_build:
    commands:
      - echo Build completed!

artifacts:
  files:
    - simple-web-server/**
  discard-paths: yes
