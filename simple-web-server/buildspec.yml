version: 0.2

phases:
  install:
    commands:
      - echo Nothing to install
  build:
    commands:
      - echo Nothing to build
  post_build:
    commands:
      - echo Build completed!

artifacts:
  files:
    - '**/*'
  discard-paths: yes

